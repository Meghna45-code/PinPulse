{
  "buildCommand": "cd frontend && npm install && NODE_OPTIONS=\"--max-old-space-size=8192\" npm run build",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    }
  ]
}
